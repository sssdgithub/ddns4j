server:
  port: 10000
spring:
  profiles:
    active: h2  # h2,mysql
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
ddns4j:
  publicAccessDisabled: true
#日志目录
logging:
  file:
    path: ./
    name: ddns4j.log
